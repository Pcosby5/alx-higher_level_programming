#!/usr/bin/node

// Define a function called add and use  exports to make it accessible outside the script
exports.add = function (a, b) {
  return a + b; // Return the addition of the two integers passed as arguments
};
